<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- MySQL Connection -->
        <!-- Note: System properties (-Dhibernate.connection.*) override these values -->
        <!-- This allows using .env file via scripts/seed.sh or scripts/run.sh -->
        <!-- WARNING: Do not commit real credentials. Use .env file for production. -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">
            jdbc:mysql://127.0.0.1:3306/hibernate_test?serverTimezone=UTC
        </property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">password</property>

        <!-- Basic Configuration -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL5InnoDBDialect</property>        
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Mapping -->
        <mapping resource="model/MappingClasses.hbm.xml"/>

    </session-factory>
</hibernate-configuration>
